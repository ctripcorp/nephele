server_config_path = "server.toml"

[interpret]
type = "inline"

#type = "plugin"
#path = "[your-interpret].so"

[process]

[storage]
type = "inline"
root = "/home/phyxdown/nephele/image/"

#path = "[your-storage].so"
#endpoint = "[endpoint]"
#bucketname = "[bucketname]"
#accessKeyId = "[access-key-id]"
#accessKeySecret = "[access-key-secret]"
